<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.congcong.algomentor.auth.repository.mybatis.AuthUserMapper">
  <resultMap id="OAuthAccountRowMap" type="org.congcong.algomentor.auth.repository.mybatis.model.OAuthAccountRow">
    <constructor>
      <idArg column="id" javaType="java.lang.Long"/>
      <arg column="user_id" javaType="java.lang.Long"/>
      <arg column="provider" javaType="java.lang.String"/>
      <arg column="provider_subject" javaType="java.lang.String"/>
      <arg column="email_at_provider" javaType="java.lang.String"/>
      <arg column="display_name_at_provider" javaType="java.lang.String"/>
      <arg column="avatar_url_at_provider" javaType="java.lang.String"/>
      <arg column="created_at" javaType="java.time.Instant"/>
      <arg column="updated_at" javaType="java.time.Instant"/>
    </constructor>
  </resultMap>

  <resultMap id="PasswordCredentialRowMap" type="org.congcong.algomentor.auth.repository.mybatis.model.PasswordCredentialRow">
    <constructor>
      <idArg column="id" javaType="java.lang.Long"/>
      <arg column="user_id" javaType="java.lang.Long"/>
      <arg column="password_hash" javaType="java.lang.String"/>
      <arg column="created_at" javaType="java.time.Instant"/>
      <arg column="updated_at" javaType="java.time.Instant"/>
    </constructor>
  </resultMap>

  <select id="findOAuthAccount" resultMap="OAuthAccountRowMap">
    select id,
           user_id,
           provider,
           provider_subject,
           email_at_provider,
           display_name_at_provider,
           avatar_url_at_provider,
           created_at,
           updated_at
    from auth_oauth_accounts
    where provider = #{provider}
      and provider_subject = #{providerSubject}
  </select>

  <insert id="insertPasswordCredential"
          parameterType="org.congcong.algomentor.auth.repository.mybatis.model.PasswordCredentialRow"
          useGeneratedKeys="true"
          keyProperty="id">
    insert into auth_password_credentials (
      user_id,
      password_hash,
      created_at,
      updated_at
    ) values (
      #{userId},
      #{passwordHash},
      #{createdAt},
      #{updatedAt}
    )
  </insert>

  <select id="findPasswordCredentialByEmailNormalized" resultMap="PasswordCredentialRowMap">
    select credential.id,
           credential.user_id,
           credential.password_hash,
           credential.created_at,
           credential.updated_at
    from auth_password_credentials credential
    inner join auth_users users on users.id = credential.user_id
    where users.email_normalized = #{emailNormalized}
  </select>

  <insert id="insertOAuthAccount"
          parameterType="org.congcong.algomentor.auth.repository.mybatis.model.OAuthAccountRow"
          useGeneratedKeys="true"
          keyProperty="id">
    insert into auth_oauth_accounts (
      user_id,
      provider,
      provider_subject,
      email_at_provider,
      display_name_at_provider,
      avatar_url_at_provider,
      created_at,
      updated_at
    ) values (
      #{userId},
      #{provider},
      #{providerSubject},
      #{emailAtProvider},
      #{displayNameAtProvider},
      #{avatarUrlAtProvider},
      #{createdAt},
      #{updatedAt}
    )
  </insert>

  <update id="updateOAuthAccountProfile">
    update auth_oauth_accounts
    set email_at_provider = #{emailAtProvider},
        display_name_at_provider = #{displayNameAtProvider},
        avatar_url_at_provider = #{avatarUrlAtProvider},
        updated_at = #{updatedAt}
    where id = #{accountId}
  </update>

</mapper>

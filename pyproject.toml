[project]
name = "algo-mentor-tests"
version = "0.1.0"
description = "Smoke and evaluation test tooling for algo-mentor."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
